{
  "rules": {
    /// TODO 8:Set rules to "com_list" node, let it only can be read/write when user is login
    "room_list":{
      "com_list":{
          ".write":"auth != null",
          ".read":"auth != null"
      },
      ".write":"auth != null",
      ".read":"auth != null"
    },
    ".write":false,
    ".read":false
  }
}